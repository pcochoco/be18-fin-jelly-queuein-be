## 명령 실행 및 권한 정책

- 코드 분석을 위해 현재 workspace 내부 파일을 읽는 경우 추가 권한을 요청하지 않는다.
- 파일 조회와 검색은 가능한 한 workspace 내부에서 수행한다.
- 코드 검색은 `rg`, `rg --files` 등 단순한 읽기 전용 명령을 우선한다.
- 단순 파일 확인을 위해 인터넷 접근 권한을 요청하지 않는다.
- 요청받지 않은 workspace 외부 파일은 읽지 않는다.
- 읽기 작업이 sandbox 때문에 실패하면 즉시 권한 상승을 요청하지 말고, 차단된 경로와 이유를 먼저 알린다.
